export const setLoading = loading => ({
  type: "SET_LOADING",
  loading
});

export const setOrders = orders => ({
  type: "SET_ORDERS",
  orders
});

export const setNotes = notes => ({
  type: "SET_NOTES",
  notes
});
